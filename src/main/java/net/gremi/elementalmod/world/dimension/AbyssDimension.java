package net.gremi.elementalmod.world.dimension;

import net.gremi.elementalmod.ElementalMod;
import net.minecraft.registry.Registerable;
import net.minecraft.registry.RegistryKey;
import net.minecraft.registry.RegistryKeys;
import net.minecraft.util.Identifier;
import net.minecraft.world.World;
import net.minecraft.world.dimension.DimensionOptions;
import net.minecraft.world.dimension.DimensionType;

import java.util.OptionalLong;

public class AbyssDimension {
    /**public static final RegistryKey<DimensionOptions> ABYSSDIM_KEY = RegistryKey.of(RegistryKeys.DIMENSION,
            Identifier.of(ElementalMod.MOD_ID, "abyssdim"));
    public static final RegistryKey<World> ABYSSDIM_LEVEL_KEY = RegistryKey.of(RegistryKeys.WORLD,
            Identifier.of(ElementalMod.MOD_ID, "abyssdim"));
    public static final RegistryKey<DimensionType> ABYSSDIM_TYPE = RegistryKey.of(RegistryKeys.DIMENSION_TYPE,
            Identifier.of(ElementalMod.MOD_ID, "abyssdim_type"));

    public static void boostrapType(Registerable<DimensionType> context) {
        context.register(ABYSSDIM_TYPE, new DimensionType(
                OptionalLong.of(12000),
                false,
                false,
                false,
                true,
                1,
                true,
                false,
                0,
                256,
                256,
                B

        ))
    }**/
}
